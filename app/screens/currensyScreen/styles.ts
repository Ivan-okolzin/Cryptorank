import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
  header: {
    fontSize: 24,
    fontWeight: 'bold',
    padding: 20,
  },
  searchInput: {
    fontSize: 18,
    padding: 10,
    margin: 20,
    borderColor: '#ccc',
    borderWidth: 1,
    borderRadius: 10,
  },
});
